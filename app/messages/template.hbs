<div class="box bg-gray row collapse">
  <div class="medium-5 columns">
    <section class="bg-white text-right">
      {{link-to "New Message" "messages.compose" class="button clear" }}
    </section>
    <section>
      <form class="form-profile">
        {{input value=key placeholder="Search by Name" class="no-margin search-input"}}
      </form>
    </section>
    <section class="container">
      <ul class="no-bullet user-link-list bg-white">
        {{#each conversations as |item| }}
        {{#if item.content}}
        <li>
          {{#link-to "messages.conversation" item.id class="no-color"}}
          <div class="row user">
            <div class="columns medium-8">
              <div class="avatar" style={{htmlSafeImage item.avatar }}>
                <img src= {{item.avatar}} class="hide">
              </div>
              <div class="user-info">
                <a class="name">{{ item.fullName }}</a>
              </div>
            </div>
            <div class="column medium-4 text-right">
              <div>
                <div class="timestamp">{{ moment-format item.updatedAt "h:ma" }}</div>
              </div>
            </div>
          </div>
          <div class="message-body truncate">{{{item.content}}}</div>

          {{/link-to}}
        </li>
        {{/if}}
        {{/each}}
      </ul>
    </section>
  </div>
  <div class="medium-7 columns border-l message-pane">
    {{ outlet }}
  </div>
</div>
