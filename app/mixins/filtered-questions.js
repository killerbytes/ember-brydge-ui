import Ember from 'ember';

export default Ember.Mixin.create({
	// filteredFromQuestions: Ember.computed('fromQuestions', function(){
	// 	console.log(this)
	// 	console.log(this.get('fromQuestions'))
	// 	var t = this.get('fromQuestions').toArray();
	// 	return _.filter(t, function(i){
	// 		if(i.get('answer')) return true;
	// 	})
	// 	// return this.get('fromQuestions')

	// }),
});
