<nav>
  {{#link-to 'index'}} Home {{/link-to}}
  {{#if session.isAuthenticated}}
   <a {{action 'logout' }}> Logout </a>
  {{else}}
    {{#link-to 'login'}} Login {{/link-to}}
  {{/if}}
</nav>

<h2 id="title">Brydge Newsfeed</h2>

{{outlet}}
