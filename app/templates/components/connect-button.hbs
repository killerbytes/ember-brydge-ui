{{#unless isConnected}}

  {{#if isPending}}
    <span>Pending [{{isConnected}}]</span>
  {{else}}
    <a class="button" {{action "connect" account}} >Connect {{account.name}}</a>
  {{/if}}

{{/unless}}
