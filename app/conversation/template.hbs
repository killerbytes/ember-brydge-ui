<div>
	<h3>Messages for {{model.id}} ({{model.messages.length}})</h3>

	{{#each model.messages as |message|}}
			<div class="callout secondary">
				[{{message.to.name}}]
				<p>{{ message.content }}</p>
			</div>
	{{/each}}
</div>
