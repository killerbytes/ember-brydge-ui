      {{#each newsfeed as |post|}}
          
          {{newsfeed-item post=post avatar=avatar}}

        {{else}}
        <div class="small-12 column">
          <p>(No records found)</p>
        </div>
      {{/each}}
