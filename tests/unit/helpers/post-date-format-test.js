import { postDateFormat } from 'web/helpers/post-date-format';
import { module, test } from 'qunit';

module('Unit | Helper | post date format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postDateFormat([42]);
  assert.ok(result);
});
